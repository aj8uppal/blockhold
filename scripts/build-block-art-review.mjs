import {build} from 'esbuild'
import {readFileSync,writeFileSync,mkdirSync,cpSync} from 'node:fs'
import {chromium} from '@playwright/test'
import {createServer} from 'vite'

const root=process.cwd(),dir=`${root}/reviews/seraph-block-studies`,lavish=`${root}/.lavish/seraph-block-studies`
const art=[
  {id:'guardian',letter:'A',name:'Stone Seraphs',note:'Carved guardians',desc:'A recognizably angelic statue. Broad wings, a quiet stone body, and a strong sun or eclipse crown.',trade:'Preserves the Seraph identity most directly.',solar:'Raised wings · sun crown',void:'Hooded face · heavy wings'},
  {id:'sanctum',letter:'B',name:'Sanctum Spires',note:'Sacred architecture',desc:'A chapel grows into an open sun temple or an imposing dark gate. Uses the masonry and roof shapes of the existing towers.',trade:'The closest match to the other buildings; less of a living idol.',solar:'Open shrine · golden roof',void:'Split roof · abyssal gateway'},
  {id:'relic',letter:'C',name:'Crowned Relics',note:'Magical monuments',desc:'A suspended relic held by broad stone supports. The sunstone opens outward; the dark core sits inside a heavy crown.',trade:'The boldest magical silhouette, with fewer decorative parts.',solar:'Sunstone · gilded cradle',void:'Black core · broken crown'},
]
const fire=[
  {id:'hearth',letter:'1',name:'Hearthfire',note:'Compact flame stacks',desc:'Connected orange tongues with warm cores and leaning, stepped tips.',trade:'A balance of recognizable flame height and clear enemy silhouettes.'},
  {id:'torch',letter:'2',name:'Forked Torchfire',note:'Taller, sculpted flames',desc:'Fewer, more expressive tongues that split and sway in continuous motion.',trade:'The strongest flame silhouette; occupies more space around enemies.'},
  {id:'cinder',letter:'3',name:'Cinderfield',note:'Low flames & coals',desc:'Short flames with a few rising cuboid embers, close to the road surface.',trade:'The clearest view of enemies during crowded endless waves.'},
]
const forms=[['T1','Seraph Idol'],['T2','Seraph Ascendant'],['T3','Seraph Sovereign'],['Solar · T4','Solar Seraph'],['Solar · T5','The Dawnbringer'],['Solar · T6','Helios Engine'],['Void · T4','Void Seraph'],['Void · T5','The Eventide'],['Void · T6','Event Horizon']]
const bundle=await build({entryPoints:[`${dir}/viewer.ts`],bundle:true,write:false,format:'iife',minify:true,target:'es2022',logLevel:'silent'})
const viewer=readFileSync(`${dir}/viewer.html`,'utf8').replace('<script type="module" src="./viewer.ts"></script>',()=>`<script>__STUDY_QUERY__</script><script>${bundle.outputFiles[0].text.replaceAll('</script','<\\/script')}</script>`)
const css=`
:root{color-scheme:dark;--bg:#10191e;--card:#19252b;--line:#ffffff20;--text:#f1f2ed;--muted:#adbcc2;--gold:#e7c485;--violet:#c0a2e5}*{box-sizing:border-box}html{scroll-behavior:smooth;scroll-padding-top:24px}body{margin:0;background:var(--bg);color:var(--text);font:15px/1.55 -apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif}main{max-width:1460px;margin:auto;padding:30px 36px 60px}header{display:flex;justify-content:space-between;align-items:center;gap:16px;border-bottom:1px solid var(--line);padding-bottom:20px}header b{letter-spacing:.15em;font-size:12px}.nav{display:flex;gap:8px;flex-wrap:wrap}a{color:var(--gold)}button,a.btn,select,textarea{font:inherit}button,a.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;border:1px solid var(--line);background:#ffffff08;color:var(--text);border-radius:10px;min-height:44px;padding:10px 15px;text-decoration:none;cursor:pointer}button:hover,a.btn:hover{background:#ffffff12;border-color:#e7c48566}button:focus-visible,a:focus-visible,select:focus-visible,textarea:focus-visible,summary:focus-visible{outline:3px solid var(--gold);outline-offset:3px}.primary{background:var(--gold)!important;color:#1c2427!important;border-color:var(--gold)!important}.eyebrow{color:var(--gold);text-transform:uppercase;letter-spacing:.13em;font-size:11px;font-weight:700}h1{font-size:clamp(34px,4.5vw,62px);line-height:1.1;letter-spacing:-.045em;margin:22px 0 14px}h2{font-size:30px;letter-spacing:-.035em;line-height:1.2;margin:0}h3{font-size:22px;letter-spacing:-.02em;margin:8px 0}p{color:var(--muted);margin:10px 0}.intro{max-width:780px;font-size:17px}.section-head{display:flex;justify-content:space-between;align-items:end;gap:20px;margin:50px 0 20px}.section-head p{max-width:640px;margin:8px 0 0}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:18px}.card{background:var(--card);border:1px solid var(--line);border-radius:17px;overflow:hidden;min-width:0}.card .copy{padding:20px}.card p{font-size:14px}.card .description{min-height:88px}.art{display:block;width:100%;aspect-ratio:1000/650;object-fit:cover;background:#8caf76}.media-button{display:block!important;border:0!important;border-radius:0!important;padding:0!important;width:100%;line-height:0}.branch-labels{display:flex;justify-content:space-around;background:#121e24;padding:9px 12px;font-size:12px;color:var(--gold)}.branch-labels span:last-child{color:var(--violet)}.actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:16px}.actions>*{flex:1;font-size:13px}.caption{font-size:12px;color:var(--muted)}details{border:1px solid var(--line);border-radius:15px;margin:18px 0;background:#142027;overflow:hidden}summary{cursor:pointer;padding:20px 22px;font-size:18px;font-weight:650;min-height:64px}summary small{font-size:13px;color:var(--muted);font-weight:400;margin-left:12px}.detail-body{padding:0 22px 24px}.progression{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}figure{margin:0;min-width:0;background:#1b282d;border-radius:12px;overflow:hidden}figure img{display:block;width:100%;aspect-ratio:1;object-fit:cover}figcaption{padding:12px 15px}figcaption b,figcaption span{display:block}figcaption b{font-size:12px;color:var(--gold);margin-bottom:4px}figcaption span{font-size:14px}.row-label{margin:28px 0 12px;font-size:14px;color:var(--gold);font-weight:650}.row-label.void{color:var(--violet)}.wide{width:100%;border-radius:12px;display:block}.context-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin-top:16px}.callout{padding:22px 25px;border-left:3px solid var(--gold);background:#e7c48507;margin:30px 0}.callout p{max-width:90ch;font-size:14px}.form-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}label{font-size:13px;color:var(--muted);display:block}select,textarea{display:block;width:100%;color:var(--text);background:#152127;border:1px solid #ffffff35;border-radius:10px;padding:12px;margin-top:7px;min-height:46px}textarea{min-height:90px;resize:vertical}.choice-form{padding:24px;border:1px solid var(--line);border-radius:16px;background:var(--card)}.choice-form .actions{justify-content:start}.choice-form .actions>*{flex:initial}.choice-form output{font-size:13px;color:var(--gold)}footer{border-top:1px solid var(--line);padding-top:24px;margin-top:45px;color:var(--muted);font-size:12px}dialog{padding:0;border:1px solid #ffffff40;border-radius:15px;width:min(1200px,96vw);max-width:none;height:min(860px,94dvh);max-height:none;background:#122027;color:var(--text)}dialog::backdrop{background:#05090ddd;backdrop-filter:blur(4px)}.dialog-bar{height:60px;display:flex;align-items:center;justify-content:space-between;padding:8px 14px;gap:12px}.dialog-bar strong{font-size:14px}iframe{border:0;width:100%;height:calc(100% - 60px);display:block}.tip{font-size:12px;color:var(--muted)}img,video{max-width:100%}.grid>*,.context-grid>*,.form-grid>*{min-width:0}h1,h2,h3,p,figcaption,a,button{overflow-wrap:anywhere}.sheet{max-width:1600px;padding:40px}.sheet h1{font-size:42px}.sheet .progression{gap:16px}.sheet figure{background:#1b282d}.sheet figcaption{min-height:74px}.sheet .row-label{font-size:17px}.sheet header{padding:0 0 20px}
@media(max-width:1000px){main{padding:24px}.grid{gap:12px}.card .copy{padding:15px}.card .description{min-height:120px}.form-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:700px){main{padding:18px 14px 40px}header{align-items:start;flex-direction:column}.grid,.context-grid{grid-template-columns:1fr}.card .description{min-height:0}.section-head{align-items:start;flex-direction:column;margin-top:35px}.progression{gap:6px}.detail-body{padding:0 10px 16px}figcaption{padding:9px 8px}figcaption b{font-size:10px}figcaption span{font-size:12px}summary{padding:17px 14px}summary small{display:block;margin:4px 0 0 15px}.form-grid{grid-template-columns:1fr 1fr}.choice-form{padding:17px}dialog{width:100vw;height:100dvh;border-radius:0}.dialog-bar{height:58px}.nav a{font-size:13px;padding:8px 11px}h2{font-size:27px}.sheet{min-width:1100px}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}
`
const renderRows=(id,interactive=true)=>[0,1,2].map(row=>`<div class="row-label ${row===2?'void':''}">${['Shared foundation · Tiers 1–3','Solar path · Tiers 4–6','Void path · Tiers 4–6'][row]}</div><div class="progression">${forms.slice(row*3,row*3+3).map(([label,name],i)=>`<figure>${interactive?`<button class="media-button" data-model="${id}" data-form="${row*3+i}" aria-label="Inspect ${name} in 3D">`:''}<img src="renders/${id}-${row*3+i}.png" alt="${label} ${name}, ${id} direction" loading="${interactive?'lazy':'eager'}">${interactive?'</button>':''}<figcaption><b>${label}</b><span>${name}</span></figcaption></figure>`).join('')}</div>`).join('')
for(const d of art)writeFileSync(`${dir}/sheet-${d.id}.html`,`<!doctype html><html lang="en"><meta charset="utf-8"><title>${d.name} · Full Seraph sheet</title><style>${css}</style><main class="sheet"><header><b>BLOCKHOLD / ART STUDY ${d.letter}</b><span>September 13 · Review candidate</span></header><h1>${d.name}</h1><p>${d.desc}</p>${renderRows(d.id,false)}<footer>All nine forms · Actual game geometry, lighting, materials and tier scale · Close-ups framed for detail; battlefield views retain game scale</footer></main></html>`)
const seraphCards=art.map(d=>`<article class="card"><div class="copy"><span class="eyebrow">${d.letter} · ${d.note}</span><h3>${d.name}</h3><p class="description">${d.desc}</p></div><button class="media-button" data-model="${d.id}" data-form="4" aria-label="Inspect ${d.name}"><img class="art" src="renders/${d.id}-pair.png" alt="${d.name}: Solar and Void tier five beside each other"></button><div class="branch-labels"><span>${d.solar}</span><span>${d.void}</span></div><div class="copy"><p>${d.trade}</p><div class="actions"><button class="primary" data-model="${d.id}" data-form="4">Inspect in 3D</button><a class="btn" href="#suite-${d.id}" data-expand="suite-${d.id}">All 9 upgrades ↓</a></div></div></article>`).join('')
const suites=art.map(d=>`<details id="suite-${d.id}"><summary>${d.letter} · ${d.name}<small>Full upgrade sheet · 9 forms</small></summary><div class="detail-body"><div class="actions"><a class="btn" href="renders/sheet-${d.id}.png" download>Download full sheet</a><button data-model="${d.id}" data-form="5" data-view="context">Compare with existing towers</button></div>${renderRows(d.id)}<div class="row-label">In the battlefield · Solar tier six</div><img class="wide" src="renders/${d.id}-context.png" alt="${d.name} next to current archer, mage, cannon and barracks models" loading="lazy"><p class="caption">Same tier scale as the game. Every battlefield view uses the same camera distance.</p></div></details>`).join('')
const fireCards=fire.map(d=>`<article class="card"><div class="copy"><span class="eyebrow">${d.letter} · ${d.note}</span><h3>${d.name}</h3><p>${d.desc}</p></div><video class="art" controls muted loop playsinline preload="metadata" poster="renders/fire-${d.id}.png" aria-label="${d.name} animation"><source src="renders/fire-${d.id}.webm" type="video/webm"></video><div class="copy"><p>${d.trade}</p><div class="actions"><button class="primary" data-fire="${d.id}">Inspect in 3D</button><button data-fire="${d.id}" data-view="context">Battlefield view</button></div></div></article>`).join('')
const select=(name,label,items)=>`<label>${label}<select name="${name}"><option value="">Choose after reviewing</option>${items.map(d=>`<option value="${d.id}">${d.name}</option>`).join('')}<option value="revise">Another pass</option></select></label>`
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Blockhold · Seraph & fire art review</title><style>${css}</style></head><body><main><header><b>♜ BLOCKHOLD / ART DIRECTION</b><nav class="nav" aria-label="Review sections"><a class="btn" href="#seraph">Seraph</a><a class="btn" href="#fire">Fire</a><a class="btn" href="#choose">Your picks</a></nav></header><h1>One world. A shared visual style.</h1><p class="intro">Three complete Seraph suites and three fire treatments. Larger blocks, quieter detail, and silhouettes that belong beside the existing towers.</p><p class="caption">Review candidates · September 13, 2026 · The live art has not been replaced</p><section id="seraph"><div class="section-head"><div><span class="eyebrow">01 / Seraph</span><h2>Holy in the light. Ominous in the dark.</h2><p>The cards show both tier-five branches. Expand a suite for all nine forms, or orbit the actual voxel models in 3D.</p></div></div><div class="grid">${seraphCards}</div><div class="callout"><b>Judge the silhouette first.</b><p>Solar uses pale stone, raised forms and golden crowns. Void uses dark mass, shadowed openings and restrained violet light. Upgrades add a distinct architectural or sculptural feature; fine crystal facets and clouds of trim are removed.</p></div>${suites}<details><summary>Current live Seraph<small>Close-up reference</small></summary><div class="detail-body"><img class="wide" src="renders/current-pair.png" alt="Current live Solar and Void tier-five Seraphs"><div class="actions"><button data-model="current" data-form="4">Inspect current art</button></div>${renderRows('current')}</div></details></section><section id="fire"><div class="section-head"><div><span class="eyebrow">02 / Fire</span><h2>Flames that belong on this battlefield.</h2><p>Play each loop to see the motion. These are solid 3D block forms with a small glowing core. The same enemy and lighting appear in every option.</p></div></div><div class="grid">${fireCards}</div><details><summary>Current live fire<small>Fine, camera-facing flame reference</small></summary><div class="detail-body"><video class="wide" controls muted loop playsinline preload="metadata" poster="renders/fire-current.png"><source src="renders/fire-current.webm" type="video/webm"></video><div class="actions"><button data-fire="current">Inspect current fire</button></div></div></details><details><summary>Fire at gameplay distance<small>Enemy visibility beside current towers</small></summary><div class="detail-body">${fire.map(d=>`<h3>${d.name}</h3><img class="wide" src="renders/fire-${d.id}-context.png" alt="${d.name} with current towers at battlefield distance" loading="lazy">`).join('')}</div></details></section><section id="choose"><div class="section-head"><div><span class="eyebrow">03 / Your direction</span><h2>Pick a suite, or combine branches.</h2><p>You can choose different styles for the shared tiers, Solar, and Void. Selections stay local until you queue your feedback.</p></div></div><form class="choice-form" id="choices" data-lavish-question="blockhold-art-direction"><div class="form-grid">${select('early','Seraph · T1–T3',art)}${select('solar','Solar · T4–T6',art)}${select('void','Void · T4–T6',art)}${select('fire','Fire treatment',fire)}</div><label style="margin-top:17px">What should change?<textarea name="notes" placeholder="For example: A’s wings, B’s base, and a darker Void crown…"></textarea></label><div class="actions"><button class="primary" type="submit">Queue my feedback</button><button type="button" id="copy">Copy feedback</button></div><output id="feedback-status" aria-live="polite"></output></form></section><footer><p>These are authored voxel models rendered with Blockhold’s actual engine, materials, light and tier scale. The 3D viewer runs independently of accounts and saved battles. Attack previews demonstrate emission points and motion; they do not simulate combat balance.</p><p>Review uses Blockhold’s approved dark surfaces, gold accents and restrained sans-serif UI. Full sheets and motion loops are saved alongside this page for morning review.</p></footer></main><dialog id="inspect"><div class="dialog-bar"><strong id="inspect-title">Voxel preview</strong><button id="close-dialog" aria-label="Close 3D preview">Close ×</button></div><iframe title="Interactive Blockhold voxel art preview"></iframe></dialog><script type="application/json" id="viewer-template">${JSON.stringify(viewer).replaceAll('<','\\u003c')}</script><script>
const dialog=document.querySelector('#inspect'),frame=dialog.querySelector('iframe'),template=JSON.parse(document.querySelector('#viewer-template').textContent);let opener=null;
function inspect(mode,direction,form=5,view='studio',button){opener=button;const q=new URLSearchParams({mode,direction,form:String(form),view}).toString();frame.srcdoc=template.replace('__STUDY_QUERY__','window.__studyQuery='+JSON.stringify(q)+';');document.querySelector('#inspect-title').textContent=mode==='fire'?'Fire study · actual voxel rendering':'Seraph study · all tiers available';dialog.showModal()}
document.querySelectorAll('[data-model]').forEach(b=>b.onclick=()=>inspect('seraph',b.dataset.model,Number(b.dataset.form??5),b.dataset.view??'studio',b));document.querySelectorAll('[data-fire]').forEach(b=>b.onclick=()=>inspect('fire',b.dataset.fire,0,b.dataset.view??'studio',b));
document.querySelector('#close-dialog').onclick=()=>dialog.close();dialog.addEventListener('close',()=>{frame.srcdoc='';opener?.focus()});dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close()}});
document.querySelectorAll('[data-expand]').forEach(a=>a.onclick=()=>{document.getElementById(a.dataset.expand).open=true});
const choices=document.querySelector('#choices'),status=document.querySelector('#feedback-status');const key='blockhold-art-review-sept13';
try{const saved=JSON.parse(localStorage.getItem(key)||'{}');for(const [k,v] of Object.entries(saved))if(choices.elements[k])choices.elements[k].value=v}catch{}
choices.oninput=()=>{try{localStorage.setItem(key,JSON.stringify(Object.fromEntries(new FormData(choices))))}catch{}status.textContent='Selected locally · not queued yet'};
function answer(){const d=Object.fromEntries(new FormData(choices));return {data:d,text:['Blockhold art review:',d.early?'Shared Seraph T1–T3: '+d.early:'',d.solar?'Solar T4–T6: '+d.solar:'',d.void?'Void T4–T6: '+d.void:'',d.fire?'Fire: '+d.fire:'',d.notes?'Notes: '+d.notes:''].filter(Boolean).join('\\n')}}
async function copy(){const a=answer();try{await navigator.clipboard.writeText(a.text);status.textContent='Feedback copied. Paste it into the conversation.'}catch{status.textContent=a.text}}
document.querySelector('#copy').onclick=copy;choices.onsubmit=event=>{event.preventDefault();const a=answer();if(!Object.values(a.data).some(Boolean)){status.textContent='Choose an option or add a note first.';return}if(window.lavish?.queuePrompt){window.lavish.queuePrompt(a.text,{tag:'art-direction',text:'Seraph and fire review choices',queueKey:'blockhold-art-direction',element:choices,data:a.data});status.textContent='Queued. Use Send to Agent in Lavish to submit.'}else{copy()}};
</script></body></html>`
writeFileSync(`${dir}/index.html`,html)
const server=await createServer({root,server:{port:0,host:'127.0.0.1'},logLevel:'error'});await server.listen()
const browser=await chromium.launch({headless:true})
try{
  const page=await browser.newPage({viewport:{width:1600,height:1000}})
  for(const d of art){await page.goto(server.resolvedUrls.local[0]+`reviews/seraph-block-studies/sheet-${d.id}.html`);await page.locator('img').evaluateAll(imgs=>Promise.all(imgs.map(img=>img.decode())));await page.locator('main').screenshot({path:`${dir}/renders/sheet-${d.id}.png`})}
}finally{await browser.close();await server.close()}
mkdirSync(lavish,{recursive:true});cpSync(`${dir}/renders`,`${lavish}/renders`,{recursive:true});writeFileSync(`${lavish}/index.html`,html)
console.log('Built the review, three full sprite sheets, and a self-contained interactive viewer. Copied to .lavish/seraph-block-studies/.')
